# SYNTHETIC ONLY. No Verentis credentials belong here.
# Origins and loopback port are deliberately fixed together in compose.yaml,
# Caddyfile and browser configuration. No secrets or .env file are required.
COMPOSE_PROJECT_NAME=office-synthetic
